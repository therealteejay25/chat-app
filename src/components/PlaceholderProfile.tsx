import React from 'react'

const PlaceholderProfile = ({size}  : { size: number }) => {
  return (
    <div className={`bg-gradient-to-br from-white/4 to-white/10 rounded-full h-${size} w-${size}`}>
        
    </div>
  )
}

export default PlaceholderProfile